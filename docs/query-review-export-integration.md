# 查询、来源复核与资料导出联合审查

- 日期：2026-09-05
- 基线：`b263baff0662158259b0cb3b6e8b8d9458d5f2e8` 及其上的第 3–5 项工作树
- 范围：当前活动代码、接口、数据库迁移/传输、Web 交互、工具与规范文档
- 状态：联合审查通过；已修复发现的问题，无遗留提交阻断项

## 审查方式

全仓执行格式、Lint、类型、单元/集成、构建和制品检查，并运行完整 Chrome 回归。
人工交叉检查本轮新增/修改代码与现有运行时接线、固定工作区 HTTP、Entry 当前态、
证据读取、隐私、图谱写入、增量索引、偏好及个人数据包之间的调用路径。
历史冻结代码保持其原边界，不把历史 TM2 或真实资料验收当作本次提交门槛。
这些检查提供当前覆盖范围内的工程证据，不声称形式化证明全部代码不存在缺陷。

## 发现与修复

| 分类                    | 可复现触发与影响                                                                               | 修复与回归                                                                                                                                     |
| ----------------------- | ---------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| PRODUCT_BLOCKER，已修复 | 修改查询条件后，在 300 毫秒延迟搜索期间，上一轮结果仍可加入新范围的清单、比较或关联起点        | 共享查询控制器只向页面暴露匹配当前请求的结果；真实浏览器暂停搜索计时器，验证旧结果和操作立即失效、新响应后恢复                                 |
| PRODUCT_BLOCKER，已修复 | 翻到第二页后在搜索框按 Enter，实际返回第一页，但页码及历史游标仍停在第二页                     | 无游标查询同步重置页码和游标；浏览器覆盖重新查询、重复清除筛选及再次翻页                                                                       |
| PRODUCT_BLOCKER，已修复 | 个人数据包恢复在临时替换偏好后等待数据库，期间的新偏好写入可成功，随后恢复失败的回滚覆盖该写入 | LocalReviewPreferencesStore 将替换、数据库回调及回滚纳入同一恢复保护；与恢复重叠的读写返回既有可重试错误，不读取临时授权、不丢失已确认的新保存 |
| 文档冲突，已修复        | 部分当前能力表仍将 Association 包写为 v4，和迁移 000031 / codec v5 不一致                      | 当前真值表、架构与路线图统一为 v5，旧 v1–v4 继续可读；历史阶段的 v4 记录保留                                                                   |

恢复保护使用同一运行实例已有的工作区写入串行边界，并拒绝等待中的其他偏好访问，
避免已经持有数据库工作区锁的调用者形成文件锁/数据库锁互等。
成功、失败和失败后显式重试均以真实临时偏好文件验证；数据库回调由可控合成 gate 挂起。
跨进程共用一个外部数据根仍遵守既有 DH-012 限制，本次没有扩大该部署边界。

## 交叉边界结论

| 边界           | 核查结果                                                                                                   |
| -------------- | ---------------------------------------------------------------------------------------------------------- |
| 已存查询       | 仅保存闭合条件和可选 Entry 身份；集合 CAS 位于原子偏好更新内，重开隐私视图仍需当次选择                     |
| 查询与阅读位置 | 结果来自当前查询；非本页位置单独读取，缺失、退役和隐私不可见统一不可用；不持久化结果、比较、游标或 AI 答案 |
| 来源复核       | 同一规则用于图谱、待复核列表和 Markdown；端点版本变化/旧记录无绑定均需重核，隐藏边不因复核恢复             |
| 并发与人工权限 | 工作区锁内重验 Entry 和 override 版本；投影重建保留人工状态，结构替换不继承旧身份的复核绑定                |
| 资料导出       | 只读取最多 20 个所选条目和两端均被选中的关系；全部隐私检查先于 Blob，预览与生成重验精确版本及字节摘要      |
| 文件与证据     | 正文摘录和不可变来源分开，保留 Unicode 区间、身份及摘要；文件只写外部 exports，客户端不提供正文或路径      |
| 兼容与接线     | 31 个迁移、66 张传输表、Association v5 及旧包升级一致；新增路由保持固定工作区、安全响应头和 no-store       |
| 原有主链       | 增量索引、确定性查询、导入/拆分/标签/联系、来源返回和无 Provider 路径继续运行；没有依赖或权限扩张          |

## 最终验证

`corepack pnpm run verify` 通过：格式、Lint、全部类型检查、171 个单元测试文件中的
1,061 项测试、2 个集成测试文件中的 4 项测试、生产构建、1,187 个候选制品文件
及部署源边界检查均通过。构建仍提示既有的单个 JavaScript 分块超过 500 kB；
本次未调整告警阈值或扩展到无关打包重构。

`corepack pnpm run test:e2e` 通过完整 29 项 Chrome 回归，包含两项新查询缺陷回归。
新功能覆盖 320、390、768、1024、1440 CSS 像素，以及键盘、强制颜色和减少动效场景；
另实际查看导出的 1440/320 像素与来源复核的 1440 像素截图，未发现新增溢出或阻断操作的问题。

`corepack pnpm run performance:m2-p0d --smoke` 使用 PostgreSQL 18.6 合成数据，
通过 31 个迁移及 noop 重放，
45 次浏览分页、48 次图谱、60 次复杂查询对照、增量索引恢复、978 次复核分页对照，
以及导出精确字节、领域状态不变、锁等待中的版本变化、隐私先于 Blob 和退役拒绝。
临时容器与外部合成数据已清理。该数据库检查完成后未再修改数据库实现；
偏好恢复修复随后纳入完整 verify，以真实临时文件和可控数据库回调验证，
不将该回调测试表述为真实 PostgreSQL 并发恢复验收。

没有使用真实资料、Provider、云端实例或生产配置，没有运行标准规模性能模式或镜像/部署验收。
本次提交集成第 3–5 项及上述修复，第 1–2 项沿用基线中已提交的实现。
